# zz

i named it `zz` simply because i wanted two char to type fast. nothing else.

its a tool to manage my tmux session, i can assing each session a tag and kill each tag or create each tag sessions in one go.
